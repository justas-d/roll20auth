{
  "name": "roll20auth",
  "version": "1.1.2",
  "description": "Provides an API to authenticate with Roll20 and retrive session and firebase keys.",
  "main": "index.js",
  "scripts": {
    "test": "mocha --timeout 10000"
  },
  "author": "Justas Dabrila",
  "license": "MIT",
  "dependencies": {
    "request": "^2.88.0",
    "request-promise-native": "^1.0.5"
  },
  "devDependencies": {
    "dotenv": "^6.0.0",
    "mocha": "^5.2.0"
  }
}
